[project]
name = "verge-contracts"
version = "0.3.0"
description = "Versioned data contracts / schema registry for canonical events (spec §14 Phase 4)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["verge_contracts"]
